<Window x:Name="Address_Input_Window" x:Class="RGB_Fighters_Launcher.AddressInput"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RGB_Fighters_Launcher"
        mc:Ignorable="d"
        Title="Inserisci nuovo indirizzo download" Height="180" Width="400" ResizeMode="NoResize" Topmost="True" WindowStartupLocation="CenterScreen">
    <Grid>
        <Button x:Name="OkButton" Style="{DynamicResource MaterialDesignFloatingActionButton}" HorizontalAlignment="Center" Margin="0,90,0,0" VerticalAlignment="Top" Width="48" Height="48" Click="Button_Click">
            <materialDesign:PackIcon Kind="Check" Width="32" Height="32"/>
        </Button>
        <TextBox x:Name="AddressBox" HorizontalAlignment="Center" Height="25" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Center" Width="300" Margin="0,-60,0,0"/>
    </Grid>
</Window>
